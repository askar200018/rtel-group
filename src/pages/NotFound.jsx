import { HeaderHeight } from '../variables/vairables';

const NotFound = () => {
  return (
    <div
      style={{
        paddingTop: `${HeaderHeight}px`,
      }}>
      <h2>Not found</h2>
    </div>
  );
};

export default NotFound;
